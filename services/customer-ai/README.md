# Maestro customer AI

Stage 2 workspace for the future customer-facing Telegram administrator.

Nothing in this directory is deployed or connected to customers yet. The current
machine-readable knowledge base is deliberately marked `draft`. Run:

```powershell
npm.cmd run verify
```

The validator checks structural errors immediately and prints the exact blockers
that must be resolved before the knowledge base can become `approved`.
